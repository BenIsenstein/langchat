# LangChat

LangChat is a template repo for an AI chat app, ready to build custom LLM tools and generative UI.

- Web UI built with React
- Backend built with FastAPI, LLM calls and agentic tools built with LangChain

## Run locally

`Docker compose up`
